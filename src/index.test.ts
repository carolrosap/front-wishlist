describe('myModule', () => {
    test('should do something', () => {
        // Arrange
        const value = 42;

        // Act
        const result = 84;

        // Assert
        expect(result).toBe(84);
    });
});
